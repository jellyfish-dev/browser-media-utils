# Browser Utils Demo

## Run

```shell
# Build `react-client-sdk`. Run following command in the project root directory
npm install
npm run build

# Change directory
cd examples/demo

# Start development server
rm -rf node_modules
npm install
npm run dev
```
